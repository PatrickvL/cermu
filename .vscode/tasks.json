{
	"version": "2.0.0",
	"tasks": [
		{
			"label": "build cermu",
			"type": "shell",
			"command": "cd ${workspaceFolder}/build && make -j16 cermu",
			"group": {
				"kind": "build",
				"isDefault": true
			},
			"problemMatcher": "$gcc"
		}
	]
}